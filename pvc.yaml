apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: oc-pipelines-pvc

spec:
  accessModes:
    - ReadWriteOnce

  resources:
    requests:
      storage: 1Gi

  storageClassName: skills-network-learner